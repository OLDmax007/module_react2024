import React from 'react';
import Pagination from "../components/Pagination";

const TodosPage = () => {
    return (
        <div>
            {/*<Pagination/>*/}
        </div>
    );
};

export default TodosPage;